<h1>Repos</h1>

<h1>Members</h1>
<% @members.each do |member| %>

  <%= link_to admin_member_path(params[:organization], member.name) do %>
    <div class="member">
      <%= member.name %>
      <%= image_tag member.avatar_url, width: 200, height: 200 %>
    </div>
  <% end %>
<% end %>
